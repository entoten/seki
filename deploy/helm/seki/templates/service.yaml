apiVersion: v1
kind: Service
metadata:
  name: {{ include "seki.fullname" . }}
  labels:
    {{- include "seki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seki.selectorLabels" . | nindent 4 }}
